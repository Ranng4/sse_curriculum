# 测试报告

## 1. 测试环境

- 日期：2026-06-18
- 操作系统：Windows
- Python：3.13.7
- 测试框架：unittest
- 后端框架：Flask 3
- 执行目录：`backend/`

## 2. 测试命令

```powershell
$env:UV_CACHE_DIR='D:\Codes\sse_curriculum\.uv-cache-report'
uv run python -m unittest discover -s tests -v
```

说明：初次在仓库根目录运行会因 `app` 包路径不在导入路径中失败；测试文件设计为从 `backend/` 目录运行，因此最终在 `backend/` 下执行。

## 3. 测试结果

| 测试用例 | 结果 | 覆盖点 |
| --- | --- | --- |
| `test_admin_stats` | 通过 | 后台统计接口 |
| `test_post_review_flow` | 通过 | 帖子审核、标记、通过 |
| `test_user_management_flow` | 通过 | 用户警告、封禁、解封 |
| `test_feed_hot_rank_and_search` | 通过 | 信息流、热榜、搜索 |
| `test_post_comment_like_favorite_flow` | 通过 | 发帖、评论、点赞、收藏 |
| `test_forum_sections_and_board_crud` | 通过 | 板块分区和 CRUD |
| `test_cannot_message_self` | 通过 | 禁止给自己发私信 |
| `test_empty_conversations` | 通过 | 空会话列表 |
| `test_send_and_receive_message` | 通过 | 私信发送、会话、未读数、已读 |
| `test_follow_unfollow_and_stats` | 通过 | 关注、取关、关注统计 |

汇总结果：

```text
Ran 10 tests in 2.680s
OK
```

## 4. 覆盖范围

1. 用户注册和 token 鉴权间接覆盖。
2. 论坛板块分区和板块管理覆盖。
3. 内容系统闭环覆盖：发布、查看、评论、点赞、收藏、搜索、热榜。
4. 社交系统闭环覆盖：关注、取关、粉丝和统计。
5. 私信系统闭环覆盖：发送、会话、未读、已读。
6. 后台管理覆盖：统计、帖子审核、用户状态处理。

## 5. 缺陷和风险

1. 真实短信、邮箱、OAuth、人脸识别和 KYC 服务未接入，当前为课程设计预留接口。
2. 未进行并发压测，不能证明高并发场景下的性能表现。
3. 前端主要通过页面联调验证，未补充前端组件自动化测试。
4. 内容合规审核目前以后台流程为主，自动敏感词和反操纵规则可继续增强。

## 6. 结论

核心功能测试全部通过，系统满足课程设计对可运行项目、功能闭环、代码质量和测试验证的要求。
